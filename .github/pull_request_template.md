## Summary

Describe the change in one or two sentences.

> **Note:** `serp.software` is the only active site. Public intake is staged in
> normalized D1 tables and versioned maintainer publications live under
> `d1/publications/`; catalog JSON files are forbidden.
