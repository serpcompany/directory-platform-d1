## Summary

Describe the change in one or two sentences.

> **Note:** `serp.software` is the only active site. Catalog changes use reviewed
> proposals under `d1/proposals/` and versioned publication manifests under
> `d1/publications/`; listing and category JSON files are migration inputs only.
