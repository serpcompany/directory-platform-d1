## Summary

Describe the change in one or two sentences.

> **Note:** `serp.software` and `pornvideodownloaders.com` are active D1-backed
> sites. Public intake is staged in each site's normalized D1 tables and
> versioned maintainer publications live under
> `d1/publications/`; catalog JSON files are forbidden.
