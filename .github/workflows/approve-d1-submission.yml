name: Review D1 submission

on:
  workflow_dispatch:
    inputs:
      submission_id:
        description: 'Badge-verified D1 submission UUID'
        required: true
        type: string
      site_id:
        description: 'Submission D1 tenant'
        required: true
        type: choice
        options:
          - serp.software
          - pornvideodownloaders.com
      confirmation:
        description: 'Type approve-<site>-submission-production'
        required: true
        type: string
      decision:
        description: 'Approve publishes a verified row; reject closes pending or verified intake'
        required: true
        type: choice
        options:
          - approve
          - reject

concurrency:
  group: ${{ inputs.site_id }}-production-d1
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

jobs:
  approve:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && inputs.confirmation == format('approve-{0}-submission-production', inputs.site_id)
    environment:
      name: ${{ inputs.site_id == 'pornvideodownloaders.com' && 'pornvideodownloaders-production' || 'production' }}
    steps:
      - name: Checkout reviewed source
        uses: actions/checkout@v7
      - name: Install dependencies
        uses: ./.github/actions/install
      - name: Run D1 contract tests
        run: pnpm test:d1
      - name: Back up production D1
        env: &production_env
          CI: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_REF: ${{ github.ref }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_WORKFLOW_REF: ${{ github.workflow_ref }}
          WORKER_PRODUCTION_CONFIRM: ${{ inputs.confirmation }}
          D1_SUBMISSION_APPROVAL_CONFIRM: ${{ inputs.confirmation }}
          D1_RELEASE_GENERATION: replatform
          DEPLOY_SITE_ID: ${{ inputs.site_id }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_D1_PRODUCTION_DATABASE_ID: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_ID }}
          CLOUDFLARE_D1_PRODUCTION_DATABASE_NAME: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_NAME }}
          CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_ID: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_ID }}
          CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_NAME: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PRODUCTION_DATABASE_NAME }}
          CLOUDFLARE_WORKER_PRODUCTION_NAME: ${{ secrets.CLOUDFLARE_WORKER_PRODUCTION_NAME }}
        run: pnpm tsx scripts/worker-release.ts backup production --site "${{ inputs.site_id }}"
      - name: Retain pre-approval backup
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.site_id }}-d1-pre-submission-approval-${{ github.sha }}
          path: .wrangler/backups/${{ inputs.site_id == 'pornvideodownloaders.com' && 'pornvideodownloaders-com' || 'serp-software' }}/production/${{ github.sha }}.replatform.sql
          if-no-files-found: error
          retention-days: 30
      - name: Apply submission schema migrations
        env: *production_env
        run: pnpm tsx scripts/worker-release.ts migrate production --site "${{ inputs.site_id }}"
      - name: Review D1 submission
        env: *production_env
        run: pnpm d1:approve:production -- "${{ inputs.submission_id }}" "${{ inputs.decision }}"
      - name: Close the admin review issue
        uses: actions/github-script@v8
        env:
          SUBMISSION_ID: ${{ inputs.submission_id }}
          SUBMISSION_SITE_ID: ${{ inputs.site_id }}
          SUBMISSION_DECISION: ${{ inputs.decision }}
          SUBMISSION_REVIEWER: ${{ github.actor }}
        with:
          script: |
            const marker = `<!-- d1-submission: ${process.env.SUBMISSION_SITE_ID}:${process.env.SUBMISSION_ID} -->`
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'all',
              per_page: 100
            })
            const issue = issues.find(item => !item.pull_request && item.body?.includes(marker))
            if (!issue) {
              core.notice('No GitHub review issue exists for this D1 submission.')
              return
            }
            if (issue.state === 'closed') {
              core.notice(`Review issue #${issue.number} is already closed.`)
              return
            }
            const outcome =
              process.env.SUBMISSION_DECISION === 'approve'
                ? 'approved and published'
                : 'declined without publishing'
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: `D1 submission **${outcome}** by @${process.env.SUBMISSION_REVIEWER}.`
            })
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              state: 'closed',
              state_reason: 'completed'
            })
