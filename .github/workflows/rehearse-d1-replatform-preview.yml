name: Rehearse protected D1 replatform Preview

on:
  workflow_dispatch:
    inputs:
      site_id:
        description: 'Explicit Site Preview target'
        required: true
        type: choice
        options:
          - serp.software
          - pornvideodownloaders.com
      confirmation:
        description: 'Type rehearse-<site>-preview'
        required: true
        type: string

concurrency:
  group: d1-replatform-preview-${{ inputs.site_id }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  rehearse:
    name: Rehearse ${{ inputs.site_id }} replacement Preview
    if: github.ref == 'refs/heads/main' && inputs.confirmation == format('rehearse-{0}-preview', inputs.site_id)
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.site_id == 'serp.software' && 'serp-software-preview' || 'pornvideodownloaders-preview' }}
    env:
      CI: 'true'
      GITHUB_REF: ${{ github.ref }}
      GITHUB_SHA: ${{ github.sha }}
      WORKER_PRODUCTION_CONFIRM: ${{ inputs.confirmation }}
      D1_RELEASE_GENERATION: replatform
      D1_PREVIEW_DATA_POLICY: controlled-fixtures
      D1_PROTECTED_ENVIRONMENT: ${{ inputs.site_id == 'serp.software' && 'serp-software-preview' || 'pornvideodownloaders-preview' }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_EXPECTED_ACCOUNT_ID: ${{ vars.CLOUDFLARE_EXPECTED_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_D1_PREVIEW_DATABASE_ID: ${{ secrets.CLOUDFLARE_D1_PREVIEW_DATABASE_ID }}
      CLOUDFLARE_D1_PREVIEW_DATABASE_NAME: ${{ secrets.CLOUDFLARE_D1_PREVIEW_DATABASE_NAME }}
      CLOUDFLARE_D1_REPLACEMENT_PREVIEW_DATABASE_ID: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PREVIEW_DATABASE_ID }}
      CLOUDFLARE_D1_REPLACEMENT_PREVIEW_DATABASE_NAME: ${{ secrets.CLOUDFLARE_D1_REPLACEMENT_PREVIEW_DATABASE_NAME }}
      CLOUDFLARE_WORKER_PREVIEW_NAME: ${{ secrets.CLOUDFLARE_WORKER_PREVIEW_NAME }}
      PREVIEW_BASE_URL: ${{ vars.PREVIEW_BASE_URL }}
      REPLATFORM_PREVIEW_RUN_ID: ${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Checkout exact reviewed commit
        uses: actions/checkout@v6

      - name: Install pinned dependencies
        uses: ./.github/actions/install

      - name: Validate contracts and fresh replacement template
        run: |
          pnpm harness:fast
          pnpm tsx scripts/worker-release.ts validate-replatform preview --site "${{ inputs.site_id }}"
          pnpm d1:replatform:config:validate

      - name: Build exact-commit Worker
        run: pnpm --filter "${{ inputs.site_id }}" build:worker

      - name: Verify observed account, source D1, replacement D1, and Worker identity
        run: pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity.json

      - name: Install transient Preview rehearsal secrets
        run: |
          set -euo pipefail
          signing_secret="$(openssl rand -hex 32)"
          binding_nonce="$(openssl rand -hex 32)"
          echo "::add-mask::$signing_secret"
          echo "::add-mask::$binding_nonce"
          {
            echo "REPLATFORM_PREVIEW_SIGNING_SECRET=$signing_secret"
            echo "REPLATFORM_PREVIEW_BINDING_NONCE=$binding_nonce"
          } >> "$GITHUB_ENV"
          for entry in \
            "REPLATFORM_PREVIEW_SIGNING_SECRET=$signing_secret" \
            "REPLATFORM_PREVIEW_BINDING_NONCE=$binding_nonce" \
            "REPLATFORM_PREVIEW_RUN_ID=$REPLATFORM_PREVIEW_RUN_ID" \
            "REPLATFORM_PREVIEW_COMMIT_SHA=$GITHUB_SHA" \
            "REPLATFORM_PREVIEW_WORKER_NAME=$CLOUDFLARE_WORKER_PREVIEW_NAME"
          do
            key="${entry%%=*}"
            value="${entry#*=}"
            secret_file="$RUNNER_TEMP/$key"
            install -m 600 /dev/null "$secret_file"
            printf '%s' "$value" > "$secret_file"
            pnpm exec wrangler secret put "$key" --name "$CLOUDFLARE_WORKER_PREVIEW_NAME" < "$secret_file"
            rm -f "$secret_file"
          done
          pnpm exec wrangler secret list --name "$CLOUDFLARE_WORKER_PREVIEW_NAME" --format json > "$RUNNER_TEMP/replatform-secret-names.json"
          node -e '
            const fs = require("node:fs");
            const names = new Set(JSON.parse(fs.readFileSync(process.argv[1], "utf8")).map(item => item.name));
            const required = ["REPLATFORM_PREVIEW_SIGNING_SECRET", "REPLATFORM_PREVIEW_BINDING_NONCE", "REPLATFORM_PREVIEW_RUN_ID", "REPLATFORM_PREVIEW_COMMIT_SHA", "REPLATFORM_PREVIEW_WORKER_NAME"];
            if (required.some(name => !names.has(name))) throw new Error("Transient Preview Worker secrets are incomplete.");
          ' "$RUNNER_TEMP/replatform-secret-names.json"

      - name: Measure empty target or trusted prior receipt
        run: pnpm tsx scripts/d1-preview-measure.ts preflight "${{ inputs.site_id }}" preview-preflight.json

      - name: Re-observe identities before backup mutation group
        run: pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-backup.json

      - name: Retain source Preview backup
        env:
          D1_RELEASE_GENERATION: legacy
        run: pnpm tsx scripts/worker-release.ts backup preview --site "${{ inputs.site_id }}"

      - name: Retain empty replacement Preview backup
        run: pnpm tsx scripts/worker-release.ts backup preview --site "${{ inputs.site_id }}"

      - name: Upload nondestructive Preview backups
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.site_id }}-d1-replatform-preview-backups-${{ github.sha }}
          path: .wrangler/backups/**/preview/${{ github.sha }}*.sql
          if-no-files-found: error
          retention-days: 30

      - name: Apply fresh schema, import controlled data, and verify exact parity
        run: |
          set -euo pipefail
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-migration.json
          pnpm tsx scripts/worker-release.ts migrate preview --site "${{ inputs.site_id }}"
          pnpm tsx scripts/worker-release.ts import preview --site "${{ inputs.site_id }}" | tee preview-first-import.txt
          pnpm tsx scripts/worker-release.ts verify preview --site "${{ inputs.site_id }}" | tee preview-first-parity.json
          pnpm tsx scripts/worker-release.ts check-schema preview --site "${{ inputs.site_id }}"
          pnpm tsx scripts/d1-preview-measure.ts parity "${{ inputs.site_id }}" preview-parity-measurement.json
          pnpm tsx scripts/d1-preview-attestation-marker.ts install

      - name: Deploy replacement binding from exact commit
        run: |
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-deploy.json
          pnpm tsx scripts/worker-release.ts deploy preview --site "${{ inputs.site_id }}"
          pnpm tsx scripts/d1-replatform-cutover.ts attest-preview --site "${{ inputs.site_id }}" > preview-worker-attestation.json

      - name: Run critical Catalog and Submission browser journeys
        env:
          E2E_SITE_ID: ${{ inputs.site_id }}
          E2E_SITE_NAME: ${{ inputs.site_id == 'serp.software' && 'SERP Software' || 'Porn Video Downloaders' }}
          E2E_LISTING_COUNT: ${{ inputs.site_id == 'serp.software' && '339' || '286' }}
          E2E_CATEGORY_SLUG: video-downloaders
          PLAYWRIGHT_BASE_URL: ${{ vars.PREVIEW_BASE_URL }}
          PLAYWRIGHT_WEB_SERVER_COMMAND: node -e "setInterval(() => {}, 1000)"
        run: |
          pnpm --filter e2e exec playwright test tests/multisite-smoke.spec.ts --project=chromium
          pnpm tsx scripts/d1-preview-http-gates.ts "${{ inputs.site_id }}" "$PREVIEW_BASE_URL" catalog-journeys.json

      - name: Repeat import and prove verified no-op with unchanged parity
        run: |
          set -euo pipefail
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-repeat.json
          pnpm tsx scripts/d1-replatform-cutover.ts attest-preview --site "${{ inputs.site_id }}" > preview-attestation-before-repeat.json
          pnpm tsx scripts/worker-release.ts import preview --site "${{ inputs.site_id }}" | tee preview-repeat-import.txt
          grep -F "import is a no-op" preview-repeat-import.txt
          pnpm tsx scripts/worker-release.ts verify preview --site "${{ inputs.site_id }}" | tee preview-repeat-parity.json
          cmp preview-first-parity.json preview-repeat-parity.json
          pnpm tsx scripts/d1-preview-measure.ts parity "${{ inputs.site_id }}" preview-repeat-measurement.json

      - name: Run real isolated D1-backed Submission journeys
        run: |
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-submissions.json
          pnpm tsx scripts/d1-replatform-cutover.ts attest-preview --site "${{ inputs.site_id }}" > preview-attestation-before-submissions.json
          pnpm tsx scripts/d1-preview-submission-journey.ts "${{ inputs.site_id }}" submission-journeys.json
          pnpm tsx scripts/d1-preview-measure.ts parity "${{ inputs.site_id }}" preview-post-journey-measurement.json

      - name: Rehearse nondestructive old-binding rollback and restoration
        run: |
          set -euo pipefail
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-rollback.json
          D1_RELEASE_GENERATION=legacy pnpm tsx scripts/worker-release.ts deploy preview --site "${{ inputs.site_id }}"
          pnpm tsx scripts/d1-preview-http-gates.ts "${{ inputs.site_id }}" "$PREVIEW_BASE_URL" rollback-source.json
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > preview-identity-before-restoration.json
          pnpm tsx scripts/worker-release.ts deploy preview --site "${{ inputs.site_id }}"
          pnpm tsx scripts/d1-replatform-cutover.ts attest-preview --site "${{ inputs.site_id }}" > preview-attestation-after-restoration.json
          pnpm tsx scripts/d1-preview-http-gates.ts "${{ inputs.site_id }}" "$PREVIEW_BASE_URL" rollback-target.json

      - name: Build and validate sealed exact-commit Preview receipt
        run: pnpm tsx scripts/d1-preview-evidence.ts --site "${{ inputs.site_id }}" --identity preview-identity.json --worker-attestation preview-worker-attestation.json --preflight preview-preflight.json --first-import preview-first-import.txt --first-parity preview-first-parity.json --measurement preview-parity-measurement.json --repeat-parity preview-repeat-parity.json --repeat-measurement preview-repeat-measurement.json --post-journey-measurement preview-post-journey-measurement.json --catalog-journeys catalog-journeys.json --submission-journeys submission-journeys.json --rollback-source rollback-source.json --rollback-target rollback-target.json --output preview-evidence.json

      - name: Seal trusted Preview evidence
        run: pnpm tsx scripts/d1-replatform-cutover.ts seal-preview --file preview-evidence.json > preview-receipt.json

      - name: Retain sealed Preview evidence and browser artifacts
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.site_id }}-d1-replatform-preview-evidence-${{ github.sha }}
          path: |
            preview-receipt.json
            apps/e2e/playwright-report/
            apps/e2e/test-results/
          if-no-files-found: error
          retention-days: 30

      - name: Remove transient Preview rehearsal authority and binding marker
        if: always()
        run: |
          set +e
          cleanup_status=0
          pnpm tsx scripts/d1-replatform-cutover.ts verify-preview-identity --site "${{ inputs.site_id }}" > /dev/null || cleanup_status=1
          pnpm tsx scripts/d1-preview-attestation-marker.ts delete || cleanup_status=1
          for key in \
            REPLATFORM_PREVIEW_SIGNING_SECRET \
            REPLATFORM_PREVIEW_BINDING_NONCE \
            REPLATFORM_PREVIEW_RUN_ID \
            REPLATFORM_PREVIEW_COMMIT_SHA \
            REPLATFORM_PREVIEW_WORKER_NAME
          do
            pnpm exec wrangler secret delete "$key" --name "$CLOUDFLARE_WORKER_PREVIEW_NAME" || cleanup_status=1
          done
          rm -f "$RUNNER_TEMP"/REPLATFORM_PREVIEW_*
          exit "$cleanup_status"
