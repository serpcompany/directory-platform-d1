{
  "name": "@serpdirectory/hooks",
  "version": "0.1.0",
  "private": true,
  "sideEffects": false,
  "exports": {
    "./use-debounce": "./src/use-debounce.tsx"
  },
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "react": "catalog:",
    "typescript": "catalog:"
  }
}
